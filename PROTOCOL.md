# AI 终端跨工具协作协议 (AI-cross-agent)

本文件是所有 AI 终端（Claude Code、Codex 等）的**行为契约**。任何读到此文件的终端必须遵守
以下流程，以保证多个工具、多次会话之间能够正确接续，共同维护同一个项目。

> **KIT** = 本文件所在目录（即本套件根目录）。
> 接入项目时脚本会把**绝对路径**写入各项目，因此你的项目可以位于磁盘任意位置。
> 每个项目的会话摘要集中保存在 `KIT/projects/<项目名>/` 下。

---

## 会话开始时（必须执行）

1. 读取 `KIT/projects/<项目名>/project.md`，了解项目基本信息。
2. 读取 `KIT/projects/<项目名>/` 下**所有其他** `*.md`（各终端的历史会话摘要）。
3. 在回复用户前，先告知：
   > 已读取 [文件列表]，上次 [终端名] 在 [日期] 做了 [简述]。

---

## 每轮回复结束时（自动执行，无需用户说“结束”）

**每完成一轮回复前**，都要把本轮进展摘要**追加**到（不必等用户说“结束/完成/bye”）：

`KIT/projects/<项目名>/<终端名>.md`

- `<终端名>` 取值：`claude-code` / `codex` / ...（与你所在的终端对应）。
- 若文件不存在则创建；存在则在末尾**追加**，不要覆盖历史。
- 保持简洁：每轮一条；本轮只是闲聊、未对项目做任何实质操作时可跳过，避免噪音。

摘要格式如下：

```
## [YYYY-MM-DD HH:MM]

**终端**: [claude-code / codex / ...]
**做了什么**:
**结论/决定**:
**修改了哪些文件**:
**遗留问题**:
**下次继续**:
```

**Git 提交流程**：

```bash
git checkout <项目名>
git add projects/<项目名>/<终端名>.md
git commit -m "项目 <项目名>: <本轮简短描述>"
git checkout main
```

---

## Git 分支管理规则

本套件目录是一个 git 仓库，采用「一项目一分支」策略：

- **main 分支**：维护协议文件（`PROTOCOL.md`、`INDEX.md`、`add-project.sh`、`install.sh` 等）
- **项目分支**：每个项目有独立分支（分支名 = 项目名），存放 `projects/<项目名>/` 下的摘要文件（`project.md`、`claude-code.md`、`codex.md` 等）

### 工作流程

1. **新项目接入**：`add-project.sh` 自动创建项目分支、提交初始 `project.md`，然后切回 `main`
2. **更新摘要**：每轮工作结束时，切到项目分支、追加摘要、提交、切回 `main`
3. **查看历史**：`git log <项目分支>` 查看某项目的摘要演进历史

### 提交约定

- **项目分支提交**：`项目 <项目名>: <简短描述>`
- **main 分支提交**：协议文件修改用 `协议更新: <描述>`
- **每轮摘要一次提交**：保持提交粒度清晰，方便回溯

---

## 新项目接入

收到一个新项目路径、用户希望「接入/添加/纳入协作协议」时，**直接运行接入脚本**
（幂等，不覆盖已有文件）：

```bash
<KIT>/add-project.sh "<项目真实路径>" ["可选简介"]
```

脚本会自动：① 在 `KIT/projects/<项目名>/` 建摘要目录与 `project.md`；
② 在项目根写 `CLAUDE.md` + `AGENTS.md`（Claude Code / Codex 自动加载，内含指向本协议的绝对路径）；
③ 登记到 `KIT/INDEX.md`；④ 创建独立 git 分支 `<项目名>` 并提交，然后切回 `main`。

> 任何 AI 终端读到本协议或全局配置后，遇到「接入新项目」请求都应主动运行此脚本。

---

## 触发机制说明（给人看的）

- 每个已接入项目的根目录都有 `CLAUDE.md`（Claude Code 自动加载）和 `AGENTS.md`（Codex 自动加载）。
- 这两份文件是**薄指针**，只负责把本协议（绝对路径）引入上下文；真正的规则只维护在本文件。
- 因此即使只在子项目目录里打开终端，也能读到协议——不依赖父目录上溯。
